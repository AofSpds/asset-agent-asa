# AAA engineering validation harness

This harness checks deterministic contracts, recovery behavior, permission boundaries, LLM-off operation, provider swap behavior, and integration invariants as AAA grows.

It is engineering/prevalidation evidence only. It must never self-issue `INDEPENDENT_VALIDATION_PASS` and does not authorize canonical cutover, model freeze, or production release.
