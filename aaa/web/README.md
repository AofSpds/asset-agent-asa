# Asset Agent ASA Owner Console

This is the AAA read-only Owner Console scaffold.

Initial navigation: Home, Assets, Work, Agents, Research, Validation, Decisions, History.

The scaffold is deterministic-first and must render status views without an LLM connection. It does not write canonical state. API wiring is intentionally deferred to the AAA CLI/API track after the deterministic state core is available.
