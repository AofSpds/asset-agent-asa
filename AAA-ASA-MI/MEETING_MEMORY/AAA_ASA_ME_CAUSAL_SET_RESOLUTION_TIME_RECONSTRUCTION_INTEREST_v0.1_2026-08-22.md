# AAA-ASA-ME — Causal Set resolution/time reconstruction interest

STATE = OWNER_EXPLICIT / HIGH-INTEREST_WORLDVIEW_HYPOTHESIS / NON_NORMATIVE / NOT_VALIDATED

Owner clarified:
- Current worldview hypothesis remains close to `무수한 국소 사상들의 합성망`.
- Owner sees the relation `A→B→C→D` implying `A≺D` as intuitively similar, but asks whether `A≺D` is already a lower-resolution abstraction.
- Owner wants a deeper understanding of how Causal Set Theory reconstructs/defines time from causal order, especially whether the theory truly preserves high-resolution structure.
- Owner suspects Causal Set Theory may offer a cleaner implementation substrate than the current richer local-mapping hypothesis and wants to study existing theory before inventing anything new.

Research distinctions to preserve:
- In a causal set, `A≺D` does not erase B,C; it is a transitive relation implied by the full partial order. A Hasse diagram/transitive reduction can store irreducible links while exactly recovering the full order by transitive closure (for the poset structure).
- CST does not define a universal coordinate-time label from `A≺D`. For manifold-like causets, intrinsic timelike/proper-time distance is approximated statistically by the length of a longest chain between causally related elements, with calibration depending on sprinkling density/dimension.
- Finite-density causal sets have fluctuations; proper-time recovery is not exact element-by-element at finite resolution.
- Event count approximates spacetime volume; causal order + number underlies the CST `Order + Number ~ Lorentzian Geometry` slogan.
- Spatial distance is substantially harder to reconstruct than timelike distance.
- User's local-mapping hypothesis remains richer than standard CST causal links because it includes transformation semantics/mechanism mutation; CST order/link is primarily kinematical causal structure.

Next high-value study question:
- Does the causet `link + transitive closure + longest-chain distance` provide enough structure for AAA's worldview/model requirements, or is transformation semantics needed at the primitive layer?
