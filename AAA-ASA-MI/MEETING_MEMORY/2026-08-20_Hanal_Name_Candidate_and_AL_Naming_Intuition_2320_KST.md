# 한알 Name Candidate / AL Naming Intuition Clarification

TIME = 2026-08-20 23:20 KST
STATE = WORKING_RESEARCH_MEMORY / NAMING_CANDIDATE_ONLY / NON_NORMATIVE

## 0. Purpose

Record only the Owner's naming intuition around `한알` / `AL`.

This note MUST NOT be used to infer or freeze the world-model semantics.

NOT:
- Requirement
- Design Contract
- Canonical ontology
- Primitive definition
- Boundary commitment
- Instance/Event/Relation commitment
- Validation receipt
- Owner acceptance of a model

## 1. Owner naming candidate

`한알` is a NAME CANDIDATE for a lower-level world-model concept distinct from ASA.

Its naming intuition should not be reduced to `seed`.

Owner explicitly corrected the prior seed-centered interpretation.

## 2. Owner-provided Korean-language intuition

Owner's intended intuition around the Korean word `알` includes usages such as:

- 눈알
- 닭알 / 달걀의 알
- 껍질을 가진 알
- 알맹이 / 껍질과 내부를 구별할 수 있는 일상어 감각

The owner-provided image also referenced ordinary Korean usages such as `달걀`, `알맹이`, and `알밤`.

IMPORTANT:
`알 = seed` is NOT the representative meaning intended here.

## 3. AL naming intuition

Owner previously expressed the naming intuition:

`AL = 경계를 지을 수 있는 최소단위`

This MUST currently be interpreted only as a NAMING / INTUITIVE CONCEPT.

It MUST NOT be promoted into a world-model primitive or formal semantic definition.

In particular, do NOT infer at this stage that:

- Boundary is a required primitive.
- AL is a Boundary object.
- AL is an Instance.
- AL is an Event.
- AL is a Relation.
- AL is an atom or indivisible ontological unit.
- AL has fixed inside/outside semantics.
- AL is biologically alive.
- AL means seed/germination.

## 4. Owner image / metaphor direction

The current naming intuition also includes a human-facing image such as:

- AI may become a kind of `껍질` for us,
- AI may become part of us,
- AI may become `한알` with us / for us.

These are NAME / IMAGE / INTUITION candidates only.

They do NOT establish current world-model semantics.

## 5. Critical non-commitment

Owner explicitly stated that the following are still undecided and may be discarded or restructured:

- Boundary itself may be discarded.
- Instance may or may not be a primary modeling concept.
- Event may or may not be a primary modeling concept.
- Relation may or may not be a primary modeling concept.

Therefore no naming rationale may be used to force these concepts into the future model.

Meaning should be assigned only when later modeling/research provides sufficient reason.

## 6. Current handling rule

For now preserve only:

NAME_CANDIDATE = `한알`

OWNER_NAMING_INTUITION =
`AL / 알` evokes a minimal distinguishable/boundable unit in the Owner's language intuition, including ordinary `알` usages with shell/interior imagery; it is not primarily a seed metaphor.

MODEL_SEMANTICS = OPEN

BOUNDARY_SEMANTICS = OPEN

INSTANCE_EVENT_RELATION_SELECTION = OPEN

SEMANTIC_PROMOTION = PROHIBITED_WITHOUT_LATER_REVIEW

## 7. Correction of prior ASA interpretation

Prior ASA interpretation that linked `한알` primarily to seed/germination was an over-interpretation and is withdrawn.

Historical discussion remains preserved, but current interpretation must use this clarification.
