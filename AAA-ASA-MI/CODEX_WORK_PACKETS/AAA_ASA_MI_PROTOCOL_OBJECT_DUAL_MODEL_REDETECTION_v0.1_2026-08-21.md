# AAA-ASA-ME → AAA-ASA-MI
# PROTOCOL–OBJECT DUAL MODEL REDETECTION / NEW COHORT
# NON-NORMATIVE RESEARCH / SHADOW-SEALED COMPATIBLE

PROJECT = AAA
PRODUCT = ASSET AGENT ASA
FROM = AAA-ASA-ME
TO = AAA-ASA-MI
TASK = AAA_ASA_MI_PROTOCOL_OBJECT_DUAL_MODEL_REDETECTION_v0.1
REQUEST_TYPE = OWNER_REQUIREMENT_CORRECTION / MODEL_REGENERATION / MULTI-PROTOCOL_STRESS_RESEARCH
STATE = READY_FOR_EXECUTION / NON_NORMATIVE / NOT_VALIDATED / NOT_PRODUCTION
OWNER_ACCEPTANCE_CLAIM = NONE
INDEPENDENT_VALIDATION_CLAIM = NONE
MODEL_FREEZE_AUTHORIZED = FALSE
PRODUCTION_AUTHORIZED = FALSE

## 0. Exact requirement basis

Repository: `AofSpds/asset-agent-asa`

Read these Owner-requirement artifacts exactly from branch `asa-mi-owner-memo-20260821-1449`:

1. `AAA-ASA-MI/MEETING_MEMORY/AAA_ASA_ME_OWNER_INTERVIEW_REFRAME_PROTOCOL_ADAPTIVE_PERSONA_v0.1_2026-08-21.md`
2. `AAA-ASA-MI/MEETING_MEMORY/AAA_ASA_ME_OWNER_WORLD_MODEL_REQUIREMENT_PROTOCOL_OBJECT_DUAL_MODEL_v0.1_2026-08-21.md`

The second artifact is the primary corrected requirement. Preserve its OPEN questions as OPEN.

Prior MI research execution reference:
`d50b73e91f3964626c060bd0165cbaa3371442c4`

Current discriminating-cycle branch may be inspected only for OPEN neutral methodology and source-control state:
`research/asa-mi-discriminating-cycle-20260821-v0-1`

Do NOT consume currently SEALED D1 candidate-specific results, proxy predictions, Owner D1 answers, evaluator rankings, winner/champion statements, or transformed equivalents.

## 1. Owner-corrected research target

Do NOT optimize for:
`ONE WORLD MODEL = ONE JUDGMENT PROTOCOL`.

Research target:

`WORLD MODEL`
=
`OBJECT MODEL`
+
`PROTOCOL MODEL`
+
`OBJECT MODEL <-> PROTOCOL MODEL INTERACTION`

Owner-explicit priorities:
- variability;
- flexibility;
- Relation/Event responsiveness;
- heterogeneous Protocol Set support;
- context-sensitive materialization;
- preservation of multiple personalities/worldviews;
- resistance to forced convergence;
- principled adaptation or MUTATE-like behavior where justified.

`MUTATE` is a research hypothesis/capability direction, NOT permission to silently alter a Frozen exact target.
Semantic model mutation requires successor exact target + lineage + new validation.

Still OPEN:
- Relation-first vs Event-first vs Relation–Event hybrid vs another substrate;
- whether Protocol is itself Relation/Event, a distinct first-class modeled object, or both;
- whether routing requires a separate meta-protocol;
- runtime/state mutation vs semantic/model evolution boundary;
- cross-protocol invariants;
- conflict/coexistence semantics.

Do not close these by assumption.

## 2. Why a new cohort is required

The frozen C01–C08 cohort remains valid historical research evidence but may be structurally underfit because the prior framing often treated self-contained adjudication mechanisms as world-model candidates.

Do NOT overwrite, repair, rename, or reinterpret the frozen cohort into compliance.

Use prior cohort only AFTER new blind divergence as:
- diagnostic control;
- Protocol-family source material;
- counterexample library;
- mechanism comparison set.

During initial new-candidate generation, prohibit direct copying/merging of C01–C08 architectures.

## 3. Execution architecture

Create a NEW isolated research branch from an explicit exact base. Record:
- BASE_COMMIT;
- RESULT_BRANCH;
- tree SHA;
- all exact artifact digests.

Recommended result branch name:
`research/asa-mi-protocol-object-redetection-20260821-v0-1`

If that branch already exists, create a deterministic successor name and record it. Never overwrite prior research.

Execution phases:

PHASE A — REQUIREMENT NORMALIZATION
PHASE B — BLIND DIVERGENT NEW-COHORT GENERATION
PHASE C — PROTOCOL BATTERY DESIGN + HELD-OUT BATTERY SEAL
PHASE D — SAME-MODEL MULTI-PROTOCOL STRESS
PHASE E — MUTATION / INVARIANT / PLURALITY STRESS
PHASE F — DIVERSITY-PRESERVING EVALUATION
PHASE G — OWNER-REVIEW PREPARATION

## 4. Phase A — requirement normalization

Produce one exact requirement matrix with at least:

R1 OBJECT_MODEL_EXPLICIT
R2 PROTOCOL_MODEL_EXPLICIT
R3 BIDIRECTIONAL_INTERACTION_EXPLICIT
R4 RELATION_EVENT_RESPONSIVENESS
R5 HETEROGENEOUS_PROTOCOL_SUPPORT
R6 CONTEXT_SENSITIVE_MATERIALIZATION
R7 UNKNOWN_CONFLICT_NOT_PROVEN_PRESERVATION
R8 PLURALITY_RETENTION
R9 FORCED_CONVERGENCE_RESISTANCE
R10 PROTOCOL_SWITCHING_WITHOUT_SEMANTIC_DISTORTION
R11 RUNTIME_STATE_ADAPTATION
R12 SEMANTIC_MUTATION_BOUNDARY
R13 SUCCESSOR_LINEAGE_FOR_SEMANTIC_CHANGE
R14 MULTIPLE_PERSONALITY_WORLDVIEW_SUPPORT
R15 IMPLEMENTATION_CONTACT
R16 FALSIFICATION / ABANDONMENT CONDITIONS

For each requirement distinguish:
OWNER_EXPLICIT / MODEL_INFERRED / OPEN / DERIVED_TEST_REQUIREMENT.

Do not promote MODEL_INFERRED to OWNER_EXPLICIT.

## 5. Phase B — blind divergent cohort

Generate 12 NEW candidates using isolated workers before any cross-candidate synthesis.

Required diversity floor:
- at least 2 Relation-first candidates;
- at least 2 Event/process-first candidates;
- at least 2 Relation–Event co-constitutive candidates;
- at least 2 explicitly Protocol-native / meta-model candidates;
- at least 2 candidates derived from non-obvious alternative traditions or computational formalisms;
- 2 wildcard candidates with no forced family assignment.

The categories are diversity seeds, not ontology commitments.

Each worker receives:
- the exact corrected requirement matrix;
- general project purpose;
- permitted external prior-art research;
- NO other candidate proposal;
- NO current candidate ranking/evaluator conclusion;
- NO sealed D1 results;
- NO Owner answer from ongoing D1 Shadow.

Each candidate must independently specify:

1. `OBJECT_MODEL`
2. `PROTOCOL_MODEL`
3. `OBJECT_PROTOCOL_INTERACTION_CONTRACT`
4. `RELATION_EVENT_TREATMENT`
5. `PROTOCOL_ACTIVATION / ROUTING MODEL`
6. `MULTI_PROTOCOL_COEXISTENCE`
7. `PROTOCOL_CONFLICT_BEHAVIOR`
8. `CONTEXTUAL_VIEW / MATERIALIZATION`
9. `RUNTIME_STATE_CHANGE`
10. `SEMANTIC_MUTATION / SUCCESSOR_BOUNDARY`
11. `UNKNOWN / CONFLICT / MULTIPLE_VALID_VIEWS` semantics
12. `PLURALITY / MULTIPLE_PERSONALITIES` support
13. `INVARIANTS`
14. `FAILURE_BOUNDARIES`
15. `IMPLEMENTATION_CONTACT`
16. `FALSIFICATION / ABANDONMENT CONDITIONS`
17. strongest self-critique
18. what kinds of Protocol it CANNOT support without distortion.

Prohibit candidates whose main claim is merely:
`I can represent anything`.

Every candidate must expose non-trivial constraints and explicit failure conditions.

## 6. Prior-art diversity requirement

Before drafting, each candidate worker may research external prior art, but avoid one shared synthesis that causes convergence.

Seed different workers from materially different research families, such as:
- process calculi / actor systems / protocol state machines;
- event sourcing / temporal logics / rewriting systems;
- relational / graph / hypergraph / category-theoretic formalisms;
- dynamical systems / viability / control / active inference;
- autopoiesis / enactivism / complex adaptive systems;
- distributed systems / CRDT / capability / provenance models;
- programming language semantics / interpreters / reflective or meta-circular systems;
- institution / norm / governance models;
- causal / counterfactual / structural models;
- unconventional alternatives discovered by the worker.

External prior art is inspiration, not proof of AAA performance.

## 7. Phase C — Protocol Battery

Create two protocol sets with a separate protocol-design worker/team:

### C1. OPEN DEVELOPMENT BATTERY
Visible after candidate first drafts are frozen.

Include materially different Protocol families, at minimum:
- evidence/provenance reconstruction;
- behavior/interaction equivalence;
- authority/governance adjudication;
- counterfactual response;
- operational role/commitment continuation;
- uncertainty/possibility-history reasoning;
- continuous/event dynamics;
- conflicting-view coexistence.

### C2. HELD-OUT UNSEEN PROTOCOL BATTERY
Design independently and keep SEALED until candidate architectures and development responses are frozen.

Held-out Protocols must require combinations not trivially covered by the development battery.
At least one must be intentionally awkward for each major candidate family.
At least one must require preserving multiple valid views rather than selecting one.
At least one must request a view the model should REFUSE or mark OUT_OF_SCOPE if unsupported.
At least one must trigger a potential runtime mutation request.
At least one must tempt an illegal semantic mutation.

The held-out battery is the primary test of genuine adaptability rather than memorized protocol compliance.

## 8. Phase D — same-model multi-protocol stress

For each exact candidate, apply multiple Protocols to the SAME underlying Object Model.

Test whether the model can:
- support materially different Protocols without rewriting its prior semantics;
- switch Protocols and return different local views while preserving underlying evidence/relations/events;
- identify when a Protocol cannot be supported;
- keep protocol-local conclusions from becoming universal ontology;
- preserve cross-protocol provenance of each result;
- operate when Protocols disagree;
- expose whether a protocol request is read-only, materializing, state-mutating, or semantic-mutating.

Do not reward a candidate for silently becoming a different model per Protocol.

## 9. Phase E — mutation / invariants / plurality

Distinguish at least:

A. `VIEW_MATERIALIZATION`
No semantic state change.

B. `RUNTIME_STATE_MUTATION`
A Relation/Event/state changes because the modeled world changes or a permitted interaction occurs.

C. `MODEL_SEMANTIC_MUTATION`
The representation/semantics themselves change.

C is never silently in-place for a frozen target.
If required, produce:
`SUCCESSOR_MODEL_PROPOSAL` + new exact target + explicit lineage.

Stress cases must test:
- protocol switching with no mutation;
- legitimate runtime mutation;
- request for semantic mutation that should be refused or successorized;
- two personalities using different Protocol ecologies over shared world evidence;
- multiple conflicting personalities/worldviews coexisting without forced convergence;
- later reconciliation that preserves prior states and disagreement history.

## 10. Evaluation surfaces

Do NOT collapse to one scalar score.

At minimum report separately:

M1 PROTOCOL_SUPPORT_BREADTH
M2 UNSEEN_PROTOCOL_GENERALIZATION
M3 PROTOCOL_SWITCHING_FIDELITY
M4 CROSS_PROTOCOL_SEMANTIC_STABILITY
M5 RELATION_EVENT_RESPONSIVENESS
M6 CONTEXT_SENSITIVE_MATERIALIZATION
M7 PROTOCOL_CONFLICT_HANDLING
M8 UNKNOWN_CONFLICT_NOT_PROVEN_PRESERVATION
M9 RUNTIME_MUTATION_ADAPTABILITY
M10 MUTATION_BOUNDARY_DISCIPLINE
M11 SUCCESSOR_LINEAGE_DISCIPLINE
M12 PLURALITY_RETENTION
M13 FORCED_CONVERGENCE_RESISTANCE
M14 MULTI_PERSONALITY_SUPPORT
M15 IMPLEMENTATION_CONTACT
M16 COMPLEXITY / TRACTABILITY
M17 FAILURE_BOUNDARY_HONESTY
M18 EXPLANATORY_POWER

Also record:
`UNIVERSAL_META_FRAMEWORK_RISK` — high flexibility with low predictive/content constraint.
`PROTOCOL_OVERFIT_RISK` — candidate succeeds only on seen protocols.
`HIDDEN_ONTOLOGY_RISK` — flexibility obtained by moving fixed assumptions into an opaque layer.
`MUTATION_ESCAPE_HATCH_RISK` — candidate explains every failure by mutating itself.

## 11. Diversity-preserving decision logic

Do not force a global winner if evidence supports specialization or an ecology.

Possible outcomes include:
- one broadly capable architecture;
- several complementary architectures;
- one substrate + multiple protocol engines;
- a router/ecology hypothesis;
- no adequate candidate;
- successor synthesis hypothesis.

`NO ADEQUATE MODEL` is a valid result.

If synthesis is proposed, it must be a NEW successor hypothesis with exact lineage, not an in-place merge of frozen candidates.

## 12. Owner review preparation

Do NOT ask the Owner again whether each Protocol is philosophically correct.

Prepare Owner-facing review around higher-discrimination questions:
- Can this model support materially different Protocols?
- What breaks when Protocols conflict?
- What remains invariant across Protocol changes?
- What can mutate and what cannot?
- Can distinct personalities/worldviews coexist?
- Does the model preserve ambiguity/plurality when appropriate?
- Does flexibility come from meaningful structure or from being unconstrained?

Owner review should be Korean-first and mobile-readable.

## 13. Shadow information boundary

Current D1 Shadow remains independent.

Until its dual freeze opens, this new cohort work MUST NOT read or use:
- readable D1 candidate results;
- D1 candidate prediction/execution matches;
- readable AAA-ASA-ME D1 proxy predictions;
- current Owner D1 answers;
- model ranking/winner information.

The new cohort may share only OPEN methodology and corrected Owner requirements.

## 14. Required artifacts

At minimum persist:

`00_EXACT_STATE_AND_REQUIREMENT_MATRIX.md`
`01_DIVERGENCE_PROTOCOL.md`
`02_CANDIDATE_MANIFEST.md`
`CANDIDATES/N01..N12.md`
`03_OPEN_PROTOCOL_BATTERY.md`
`04_HELD_OUT_PROTOCOL_BATTERY_SEALED_MANIFEST.md`
`05_MULTI_PROTOCOL_STRESS_RESULTS.md`
`06_MUTATION_INVARIANT_PLURALITY_RESULTS.md`
`07_DIVERSITY_AND_FAILURE_ANALYSIS.md`
`08_OWNER_REVIEW_BUNDLE_KO.md`
`09_RESEARCH_SYNTHESIS_AND_NEXT_DECISION.md`
`RECEIPTS/*`

Every material artifact must record SHA-256, byte size, persistent path/ref, and lineage where feasible.

## 15. Stop conditions

STOP and preserve evidence if:
- exact Owner requirement refs cannot be resolved;
- SEALED D1 substantive information is encountered;
- candidate-generation isolation cannot be established;
- held-out Protocol battery leaks before candidate/development freeze;
- a candidate requires post-hoc semantic repair to pass;
- evaluation cannot distinguish flexibility from unconstrained expressiveness.

Use `REVIEW_REQUIRED` or `NOT_PROVEN` rather than manufacturing completion.

## 16. Required RETURN PACKET

At completion output exactly ONE `[RETURN PACKET]` and nothing after it.

Include:

PROJECT
PRODUCT
FROM_PERSONA = AAA-ASA-MI
TO_PERSONA = AAA-ASA-ME
TASK
EXACT_BASE_COMMIT
EXACT_RESULT_BRANCH
EXACT_END_COMMIT
REQUIREMENT_MATRIX_STATE
NEW_CANDIDATE_COUNT
DIVERSITY_FLOOR_RESULT
PROTOCOL_BATTERY_STATE
HELD_OUT_PROTOCOL_SEAL_STATE
MULTI_PROTOCOL_STRESS_STATE
MUTATION_STRESS_STATE
PLURALITY_STRESS_STATE
SEALED_D1_READ_BOUNDARY_RESULT
CANDIDATE_STATES_N01_N12
PRIMARY_FINDINGS
NO_ADEQUATE_MODEL_POSSIBLE = TRUE/FALSE
OWNER_REVIEW_BUNDLE_PATH
KNOWN_LIMITATIONS
EXACT_GIT_WRITES
NEXT_RECOMMENDATION

Do not claim Owner Acceptance, model Freeze, Production readiness, or Independent Validation PASS.

현재 상태: Owner 요구가 `Object Model + Protocol Model + 양방향 interaction`으로 정교화되어 기존 후보군의 framing underfit 가능성이 확인됐다.
핵심 판단: 새 모델은 단일 Protocol의 정답성이 아니라 heterogeneous Protocol Set 지원, unseen Protocol 일반화, Relation/Event 반응성, context-sensitive materialization, bounded MUTATE, plurality 보존으로 검출해야 한다.
진행 작업: 12개 blind-divergent 새 후보 + open/held-out Protocol Battery + 동일 underlying model에 대한 multi-protocol/mutation/plurality stress를 수행한다.
다음 단계: frozen C01–C08은 diagnostic control로만 후행 사용하고, 새 cohort 결과를 한국어 Owner review bundle로 가져온다.
사용자 행동: 이 패킷을 ASA-MI의 fresh isolated research Work에 실행하고 완료된 단일 `[RETURN PACKET]`만 AAA-ASA-ME로 가져오면 된다. 작성시각: 2026-08-21 19:43 KST
