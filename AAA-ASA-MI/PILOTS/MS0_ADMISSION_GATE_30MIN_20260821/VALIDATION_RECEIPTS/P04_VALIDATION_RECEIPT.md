# P04 Validation Receipt

PROPOSAL_ID = P04

EXACT_PATH = `AAA-ASA-MI/PILOTS/MS0_ADMISSION_GATE_30MIN_20260821/PROPOSALS/P04_EVIDENCE_PATCHWORK_WORLD.md`

EXACT_AUTHORING_COMMIT_SHA = `a4d70f2bfc2cde09c656f6a9269fcc749de47934`

EXACT_TARGET_IDENTITY_CONFIRMED = YES — Git blob `f80bf86047294d2627cb78307f524ea51bb62c08`; SHA-256 `bfd2f9d392e72f22886393453092ed749bc925b5f012a0cca986ba57baef6794`.

V1_RESULT = NOT_PROVEN

V1_EVIDENCE = A1 constitutes the world-equivalent as compatible compositions of evidence-bearing claims. The target explains supportable descriptions and their provenance, but it does not show world dynamics or causal structure not already asserted inside patch payloads and compatibility policy. A6's only change is a pre-authored transformation patch. A8 itself makes standalone status conditional on not needing a hidden global state/solver; that condition is not demonstrated here, so the exact target remains compatible with an epistemic integration layer.

V2_RESULT = PASS_EVIDENCED

V2_EVIDENCE = A2 distinguishes immutable local evidence composition from P01's global term/rules and I04's global constraint assignments. It predicts a concrete advantage—preserving orphaned evidence—and a concrete failure—missing global consequences when compatibility is local.

V3_RESULT = CONCERN_NONBLOCKING

V3_EVIDENCE = A3 exposes patch schema/individuation, applicability, dependencies, conflicts, compatibility, selection, maximality, projection, and provenance as commitments. A6 nevertheless presupposes persistent identity `s1`, a common temporal order, interval endpoint semantics, and the authority of before/after claims without defining those policies. The relocation is visible but incomplete at the operational boundary.

V4_RESULT = PASS_EVIDENCED

V4_EVIDENCE = A4 preserves immutable patches, provenance, dependency/conflict edges, cutoff information, and policy versions. It reconstructs historical materializations under the historical policy and keeps later reinterpretation separate from world-facing evidence.

V5_RESULT = PASS_EVIDENCED

V5_EVIDENCE = A5 distinguishes unknown, undefined, disputed, multiple alternatives, and orphaned patches, while A7 requires completeness status and conflict/dependency reporting. Patch absence alone is not converted into falsehood.

V6_RESULT = NOT_PROVEN

V6_EVIDENCE = A6 exercises composition, conflict, dependency, transformation, and policy revision, but its central reconciliation is under-specified. `p1` claims `outside` throughout `t0..t2` and `p2` claims `chamber` throughout `t1..t2`; adding a move at `t1` does not by itself remove their interval overlap. The compatibility rule says the transform may "order the values" without defining interval endpoints, persistence, or how a transformation truncates an otherwise applicable claim. Therefore `{p1,p3,p2,p4}` is not demonstrably compatible under the written semantics.

V7_RESULT = PASS_EVIDENCED

V7_EVIDENCE = A7 contacts six common queries and distinguishes evidence available at a cutoff from supportable materializations. It reports policy reinterpretation separately and exposes completeness, orphan, conflict, context, and closed/open-domain assumptions.

V8_RESULT = PASS_EVIDENCED

V8_EVIDENCE = A8 tests causal fission/merge, continuous change, hidden global solver/state dependence, and materialization explosion. It predeclares merger beneath another world model, weakening, or rejection rather than a cosmetic patch when the central thesis fails.

V9_RESULT = NOT_PROVEN

V9_EVIDENCE = A9 records source, need, failure, and reversibility for evidence constitution, patch individuation, history, policy exposure, and open-domain handling. It omits the common time/order model, identity persistence across patches, provenance authority, maximality/query-bounding choice, and global-versus-local compatibility assumptions used by A1/A3/A6. These omissions can change which materializations exist and are therefore major.

V10_RESULT = PASS_EVIDENCED

V10_EVIDENCE = A10 provides immutable versioned schemas and policies, parent digests, migration maps, successor patches/translation views, `UNTRANSLATABLE`, and reproducible old materialization families. No old payload is silently redefined.

V11_RESULT = PASS_EVIDENCED

V11_EVIDENCE = A11 identifies concrete outputs—compatible compositions, conflict sets, dependency closure, orphaned evidence, alternative materializations, and provenance explanations—and constrains unsupported transformations from resolving conflict. These are real discriminations even if they may belong to an evidence layer.

V12_RESULT = CONCERN_NONBLOCKING

V12_EVIDENCE = A12 excludes Persona/ASA/application schemas and allows physical, social, computational, or abstract patches. Low-level reuse is plausible, but the kernel's evidential vocabulary and provenance orientation strengthen the unresolved possibility that it sits above a world substrate rather than constituting one.

STRONGEST_ADMISSION_EVIDENCE = A4, A5, A10, and A11 provide a strong non-retroactive, non-closed, provenance-preserving account of alternative evidence compositions.

STRONGEST_BLOCKER_OR_CONCERN = The proposal demonstrates an evidence/materialization architecture, not yet a standalone world model, and its A6 temporal reconciliation is not derivable from the written compatibility semantics.

ONTOLOGY_RELOCATION_FINDING = Ontology is relocated into patch boundaries, payload schemas, applicability/time, compatibility, provenance, identity, and maximality policies. Several of those policies are named but their operational meaning remains external.

MATERIAL_DISTINCTNESS_FINDING = Distinctness from P01 and I04 survives: orphan preservation and local evidence composition are genuine differences, though they support counteridea/evidence-layer status more strongly than standalone admission.

MICRO_PROBE_FINDING = NOT OPERATIONALLY CLOSED. The `p1`/`p2` validity intervals still overlap after the asserted `p3` move unless an unprovided persistence or interval-truncation rule is added.

FALSIFICATION_FINDING = Meaningful: hidden substrate/solver dependence and combinatorial explosion directly trigger merger, weakening, or rejection.

ROUTING_OUTCOME = KEEP_AS_COUNTERIDEA

REQUIRED_AUTHOR_ACTION_IF_ANY = Preserve the evidence-first thesis as a counteridea. For standalone reconsideration, create a successor that defines temporal/identity/compatibility semantics, repairs the interval probe, registers global selection assumptions, and demonstrates causal or dynamic leverage without importing it from a hidden substrate. This receipt does not carry forward automatically.
