# AAA-ASA-MI ↔ AAA-ASA-ME Shadow Information Control for Discriminating Cycle v0.1

STATE =
`ACTIVE_INFORMATION_CONTROL / TEMPORARY_SEAL / NO_PERMANENT_ISOLATION`

BASE_RESEARCH_EXECUTION_COMMIT =
`d50b73e91f3964626c060bd0165cbaa3371442c4`

## 1. Team model

AAA-ASA-MI and AAA-ASA-ME operate as one research program with temporary experimental information separation.

Default:
`SHARE`

Exception during active Shadow scene:
`SEALED`

## 2. OPEN to MI and ME

- completed historical research;
- methodology;
- source-integrity findings;
- neutral fixtures;
- neutral candidate materials;
- external prior art;
- Owner interview methodology;
- problem/tradeoff structure that does not reveal evaluator preferences.

## 3. SEALED until dual freeze

Do not expose to AAA-ASA-ME or Owner:

- current candidate evaluator preference conclusions;
- any model ranking;
- per-model Shadow prediction outcomes;
- any transformed result that effectively reveals which candidate evaluators favored.

Seal ends only after:

`PROXY_PREDICTIONS_FROZEN`
+
`OWNER_BLIND_DECISION_FROZEN`

Then:
`POST_FREEZE_OPEN`

## 4. Prediction receipt pattern

A separate blind prediction instance should produce:

- exact neutral input digest;
- candidate/material identity digest;
- prediction artifact digest;
- freeze timestamp;
- model/runtime metadata if available;
- statement that no evaluator-preference input was supplied.

Before Owner freeze, return only the receipt/digest to the shared research channel.
Do not return readable model-by-model predictions.

After Owner freeze, release the exact prediction artifact and verify it against the prior digest.

## 5. Human interview separation

Preferred interviewer:

`BLIND_INTERVIEW_INSTANCE`

It should receive:
- neutral vignette;
- bounded question sequence;
- no per-model prediction;
- no evaluator preference conclusion.

It should freeze:
- Owner choice/judgment;
- reasoning;
- objections;
- uncertainty;
- requested clarifications;
- update after controlled counterfactuals if any.

## 6. Joint post-freeze analysis

After dual freeze, MI + ME compare separately:

1. `MODEL_SCIENTIFIC_BEHAVIOR`
2. `OWNER_JUDGMENT`
3. `PROXY_PREDICTION`
4. `REASON_SYMMETRY`
5. `OBJECTION_SYMMETRY`
6. `EVIDENCE_ATTENTION`
7. `UNCERTAINTY`
8. `UPDATE_BEHAVIOR`

Do not collapse these into one score.

`OWNER_PREFERENCE != SCIENTIFIC_VALIDITY`
`PROXY_ACCURACY != MODEL_QUALITY`
`PERSUASIVENESS != WORLDVIEW_FIDELITY`
