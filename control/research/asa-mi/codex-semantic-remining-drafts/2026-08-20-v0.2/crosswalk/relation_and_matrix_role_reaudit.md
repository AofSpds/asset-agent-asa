# Relation and historical/live role re-audit

## Receipt

- START_TIME: 2026-08-20T10:08:02+09:00
- END_TIME: 2026-08-20T10:14:05+09:00
- ACTIVE_REVIEW_SECONDS: 363
- INPUTS: all 148 v0.2 relation candidates, all 53 v0.1 relation QA records, all 60 historical/live matrix rows, correction/split endpoint registries, and exact endpoint statements from v0.1

## New-relation result

Every candidate was reread as `FROM --TYPE--> TO` with its rationale and certainty. No further direction, type, endpoint, certainty, or duplicate correction survived challenge. In particular, `STRENGTHENS` was retained only where the relationship is claim support rather than an assertion of independent evidence; all known same-root corroboration remains `POSSIBLE_CORROBORATION`.

## Historical/live matrix corrections

The matrix had passed structural de-duplication but still contained role-transposition errors. Nine rows were corrected without changing the 60-row count:

- `HVL-0020` had attached a CS-prior claim to the `Observed Integration != System Interpretation != User-Defined Self Status` endpoint. It now compares that exact historical statement with the live self-reference/materialization boundary.
- `HVL-0029` had converted an open procedural-memory question into an affirmative historical claim. It now preserves the question and treats the live function/binding candidate as one narrowed branch.
- `HVL-0030`, `HVL-0033`, and `HVL-0035` now use their endpoints' actual lifecycle, audit-independence, and one-agent counterhypothesis wording.
- `HVL-0036` and `HVL-0037` now classify integration/external-definition versus human abstraction, and relationship-asset ownership versus runtime re-binding, as `NOT_COMPARABLE`; shared vocabulary no longer manufactures semantic continuity.
- `HVL-0038` now points to the actual protocol-definability endpoint and a live schema-preservation endpoint, with scope change explicit.
- `HVL-0039` no longer uses a portability-status successor or a split-required experiment bundle as if each stated a factorial test. It compares the exact historical same-Memory/different-model experiment with the exact live model/runtime split successor.

## Boundary

These are v0.2 crosswalk corrections, not additional v0.1 object corrections. They do not change the 118 predecessor correction count, 148 relation count, or any source/live/Codex provenance layer.
