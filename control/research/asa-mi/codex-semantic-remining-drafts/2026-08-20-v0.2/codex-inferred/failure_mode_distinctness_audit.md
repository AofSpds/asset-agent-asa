# Failure-mode distinctness audit

## Receipt

- `PASS_ID`: `FAILURE-MODE-DISTINCTNESS-QA-22`
- `START_TIME`: `2026-08-20T08:48:35+09:00`
- `END_TIME`: `2026-08-20T08:49:12+09:00`
- `ACTIVE_REVIEW_SECONDS`: `37`
- `OBJECTS_REVIEWED`: all 22 layer-E failure modes, their mechanisms, signals, consequences, and mitigations.

## Non-duplicate distinctions

- `FM-001` changes ambiguous meaning through crisp normalization; `FM-002` captures no substantive meaning because metadata becomes the statement; `FM-003` preserves words but strengthens epistemic class.
- `FM-004` collapses distinct claims through vocabulary; `FM-005` deletes negative scope; `FM-006` collapses provenance layers. These are independent axes of semantic loss.
- `FM-007` multiplies dependent evidence; `FM-008` overwrites prior states. One inflates support, the other erases lineage.
- `FM-020` confuses structural integrity with semantic/security validity; `FM-021` fabricates evidence of rereading; `FM-022` amputates narrative context. They can co-occur but have different mechanisms and detection evidence.
- `FM-009` is hidden transformation ownership; `FM-010` is interpretation mismatch after byte portability; `FM-011` is a specific epistemic-value collapse. The last is a specialization retained because it has distinct typed conformance tests.
- `FM-012` convergence, `FM-013` slow poisoning, and `FM-014` ghost influence are distinct trajectory failures: plurality loss, adversarial acquisition, and failed causal erasure.
- `FM-015` de facto Authority leakage and `FM-016` stale fission grants distinguish informal control from duplicated formal credentials.
- `FM-017` merge overwrite and `FM-018` self-model lock-in distinguish multi-input suppression from recursive self-confirmation.
- `FM-019` false continuity by mimicry is an evaluator failure, not merely a portability or provenance failure.

## Count-integrity conclusion

All 22 mechanisms remain materially distinct. `FM-011` is explicitly a specialization of `FM-010`, and `FM-021`/`FM-022` are research-process risks rather than Persona-runtime risks. The file therefore represents a typed risk inventory, not 22 independent empirical findings.

No new failure mode was added simply to increase the count.
