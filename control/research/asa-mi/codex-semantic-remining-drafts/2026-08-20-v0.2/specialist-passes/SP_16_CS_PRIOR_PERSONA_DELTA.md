# SP-16 — CS prior and Persona-specific delta

## Receipt

```text
PASS_ID = SP-16
PASS_PURPOSE = Re-read the existing-abstraction method and identify both reusable CS prior and the semantic remainder it cannot decide.
START_TIME = 2026-08-20T07:47:04+09:00
END_TIME = 2026-08-20T07:47:10+09:00
ACTIVE_REVIEW_SECONDS = 6
SOURCE_FILES_OPENED = live brainstorm registry; planning guidance; representation prior-art register; open-question catalog; tagging backlog; traceability matrix
SOURCE_FILE_COUNT = 6
SOURCE_BYTES_CONSIDERED = 64240
RAW_PRIMARY_SOURCE_VERIFICATION = NOT_PERFORMED
```

## Reusable prior areas

Existing abstractions can inform object/reference/version identity, state machines, events, serialization, cache/materialized views, compiler context, provenance graphs, capability/access control, dependency invalidation, rollback/compensation, distributed state, and degraded operation. The repository's AIDA, Nanopublication, RDF, PROV-O, SHACL, SBVR, and JSON-LD entries remain low-weight reference links only; no adoption or external re-verification occurred in this repository-only pass.

## Persona-specific semantic remainder

CS abstractions can represent a decision after its semantics are chosen; they do not determine:

- what is constitutive of a Persona rather than merely stored about it;
- when reconstruction change becomes identity-relevant;
- whether multiple descendants may inherit one autobiographical past;
- which conflicts a merge must preserve;
- how relationship recognition participates in continuity;
- which behavioral variance is acceptable growth;
- when influence becomes de facto Authority;
- what “same Persona” means across different evaluative relations.

## Mapping hazards

- Object identity may import a single-successor ontology.
- Event sourcing may privilege recorded history over current interpretation.
- Cache language may prejudge that a derived Current Status is disposable.
- CRDT-like merge may assume algebraic reconciliation where commitments are incommensurable.
- Capability models may secure formal grants while missing agenda control.
- Schema conformance may be mistaken for semantic validity.

## Relations recovered

1. Mature CS prior `MAY_IMPLEMENT` chosen Persona semantics.
2. CS prior `DOES_NOT_RESOLVE` metaphysical or relational continuity.
3. Persona-specific delta `DEPENDS_ON` observed explanatory loss after mapping.
4. Reference similarity `DOES_NOT_IMPLY` semantic equivalence.
5. Shape conformance `DOES_NOT_IMPLY` semantic correctness.
6. Cache classification `DEPENDS_ON` a prior canonicality decision.
7. Merge abstraction `MAY_CONSTRAIN` which contradictions remain visible.
8. Representation choice `MAY_IMPORT` hidden ontology.

## Materiality judgment

No source object is added. The pass preserves the anti-reinvention method but limits it: the Persona-specific delta cannot be defined as “whatever current schemas do not already name”; it must be demonstrated through lost explanation, prediction, or relationship meaning.
